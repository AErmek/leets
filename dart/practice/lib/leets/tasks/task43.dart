import 'dart:collection';

import 'package:practice/leets/task.dart';

class Task43 extends Task {
  @override
  void execute() {
    // TODO: implement execute
  }

  String multiply(String num1, String num2) {
    ListQueue<int> queue = ListQueue();
    //var iterator = num1

    return '';
  }

  Iterable<int> stringToIntDigits(String s) sync* {
    for (var item in s.runes) {
      yield item - 48;
    }
  }
}
